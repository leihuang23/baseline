# Fixtures

Synthetic data generators and golden fixtures live here.

Use `packages.fixtures.get_scenario(name)` for named scenarios, including the
the golden scenarios and `demo_60_day_persona`.

All outputs are deterministic for a given seed and contain synthetic structured
data only.
